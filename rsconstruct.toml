# rsconstruct build configuration for this repository.

[build]
# src_dirs entries name directories; "." (the whole tree) is a config error
reject_dot_src_dirs = true

[processor.shellcheck]
src_dirs = ["scripts", "src"]

# ─── toml / yaml / json ──────────────────────────────────────────────────────

[processor.taplo]
src_files = [".rumdl.toml", "rsconstruct.toml"]

# The Clojure CLI is not an apt package; scripts/install_deps.sh installs it
# from clojure.org. These are what that installer itself needs: a JDK to run
# Clojure and curl to download the installer.
[dependencies]
system = [
	"curl",
	"default-jdk",
]

# lint the GitHub Actions workflow files with actionlint
[processor.actionlint]
src_dirs = [".github/workflows"]

# ─── markdown ────────────────────────────────────────────────────────────────

[processor.rumdl]
src_files = ["README.md"]
